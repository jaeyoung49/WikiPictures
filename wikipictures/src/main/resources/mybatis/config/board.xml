
<!-- <?xml version="1.0" encoding="UTF-8"?>
Sql Mapper
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
<resultMap type="bvo" id="boardRM">
		<result property="memberVO.id" column="id" />
		<result property="memberVO.name" column="name" />
		
	</resultMap>
	<select id="List" resultMap="boardRM" parameterType="map">
		SELECT b.pictureDate,b.keyword,b.path,b.hits,b.comment, b.hits,
		 b.pictureSpace, m.name FROM(
		SELECT row_number() over(order by no desc) as rnum,pictu,title,hits,
		to_char(time_posted,'YYYY.MM.DD') as time_posted,id
		FROM spring_board_inst
		) b,spring_member m where b.id=m.id and rnum between #{startRowNumber} and #{endRowNumber}
		order by no desc
	</select>
	<select id="memberList" resultMap="mvo">
	select id, nickname, to_char(birth,'YYYY.MM.DD'), fav_space from member
	</select>
	<select id="reportList" resultMap="boardRM" parameterType="map">
	select b.pictureDate, b.keyword, m.id from
	</select>
	</mapper> -->